server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/media
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update


file:
  upload-dir: uploads
